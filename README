Used to statically generate http://opensource.yubico.com from the Github README's.

== Dependencies

[source,bash]
sudo apt-get install libsort-versions-perl libmime-base64-urlsafe-perl libjson-perl asciidoc libwww-perl 

== Building
Run `./build-projectpage <ProjectName>`
